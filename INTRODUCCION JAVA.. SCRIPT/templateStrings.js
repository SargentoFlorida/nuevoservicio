const nombre = 'Angelo';
const edad  = 40;
const pais = 'Colombia';

console.long('la persona es ' + nombre + ' tiene ' + edad + ' y es de' + pais);

console.long(` la persona es  ${nombre} tiene ${edad} y es de ${colombia}` );